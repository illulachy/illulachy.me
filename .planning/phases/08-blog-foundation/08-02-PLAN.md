---
phase: 08-blog-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/blog/src/pages/index.astro
  - apps/blog/src/components/PostCard.astro
  - apps/blog/src/lib/reading-time.ts
  - apps/blog/src/test/reading-time.test.ts
  - apps/blog/src/test/sort.test.ts
autonomous: true
requirements: [BLOG-01, BLOG-04, VIS-02]
must_haves:
  truths:
    - "User can view a post list page showing titles, dates, excerpts, and reading times in reverse-chronological order"
    - "Each post displays an estimated reading time in N min read format"
    - "Post list page has max-width ~680px centered layout with responsive padding"
    - "Posts load from packages/content/content/posts/ via import.meta.glob()"
  artifacts:
    - path: "apps/blog/src/pages/index.astro"
      provides: "Post list page with glob loading and reverse-chronological sort"
      contains: "import.meta.glob"
    - path: "apps/blog/src/components/PostCard.astro"
      provides: "Medium-style post row with title, excerpt, metadata, thumbnail"
      contains: "article"
    - path: "apps/blog/src/lib/reading-time.ts"
      provides: "Reading time calculation (200 wpm, min 1)"
      exports: ["calcReadingTime"]
    - path: "apps/blog/src/test/reading-time.test.ts"
      provides: "Unit tests for reading time calculation"
      contains: "describe"
    - path: "apps/blog/src/test/sort.test.ts"
      provides: "Unit tests for reverse-chronological sort"
      contains: "describe"
  key_links:
    - from: "apps/blog/src/pages/index.astro"
      to: "packages/content/content/posts/"
      via: "import.meta.glob with literal path"
      pattern: "import\\.meta\\.glob"
    - from: "apps/blog/src/pages/index.astro"
      to: "apps/blog/src/components/PostCard.astro"
      via: "component import"
      pattern: "PostCard"
    - from: "apps/blog/src/pages/index.astro"
      to: "apps/blog/src/lib/reading-time.ts"
      via: "function import"
      pattern: "calcReadingTime"
---

<objective>
Post list page: replace Phase 7 placeholder with a working post list that loads blog posts via import.meta.glob(), sorts reverse-chronologically, and renders PostCard components in a Medium-style editorial layout.

Purpose: Delivers BLOG-01 (post list with titles/dates/excerpts), BLOG-04 (reading time), and VIS-02 (responsive prose layout).
Output: Working index.astro, PostCard.astro, reading-time utility, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-blog-foundation/08-CONTEXT.md
@.planning/phases/08-blog-foundation/08-RESEARCH.md
@.planning/phases/08-blog-foundation/08-UI-SPEC.md
@.planning/phases/08-blog-foundation/08-VALIDATION.md
@.planning/phases/08-blog-foundation/08-01-SUMMARY.md

@apps/blog/src/layouts/BaseLayout.astro
@apps/blog/src/styles/global.css
@apps/blog/vitest.config.ts
@packages/content/content/posts/deep-dive-typescript.md
@.stich/layout.jpeg

<interfaces>
<!-- From Plan 01 outputs -->

From apps/blog/src/layouts/BaseLayout.astro:
  Props: { title?: string }  (default: 'Writing -- illulachy.me')
  Provides: <html>, <head>, Google Fonts, global.css, <body> with Tailwind classes
  Usage: wrap page content with <BaseLayout title="..."><slot /></BaseLayout>

Blog post D-02 frontmatter schema (in each .md file):
  title: string
  date: string (ISO 8601, e.g. "2024-01-15")
  excerpt: string
  tags: string[]
  category: string

Tailwind token classes available (from @illu/tokens):
  bg-surface-default, bg-surface-container-low, bg-surface-container-high
  text-text-primary, text-text-secondary, text-text-tertiary
  text-accent-mauve, text-interactive-default
  font-display, font-heading, font-body, font-mono
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reading time utility + sort logic + unit tests (Wave 0 + TDD)</name>
  <files>
    apps/blog/src/lib/reading-time.ts
    apps/blog/src/test/reading-time.test.ts
    apps/blog/src/test/sort.test.ts
  </files>
  <read_first>
    apps/blog/vitest.config.ts
    apps/blog/package.json
    .planning/phases/08-blog-foundation/08-VALIDATION.md
  </read_first>
  <behavior>
    Reading time tests (reading-time.test.ts):
    - 200 words input -> "1 min read" (200/200 = 1)
    - 400 words input -> "2 min read" (400/200 = 2)
    - 600 words input -> "3 min read" (600/200 = 3)
    - 50 words input -> "1 min read" (minimum floor)
    - 0 words / empty string -> "1 min read" (minimum floor)
    - 350 words input -> "2 min read" (350/200 = 1.75, rounds to 2)

    Sort tests (sort.test.ts):
    - Array of posts with dates ["2024-01-15", "2023-11-03", "2024-04-10"] sorted -> ["2024-04-10", "2024-01-15", "2023-11-03"] (newest first)
    - Single post -> returns same post
    - Empty array -> returns empty array
  </behavior>
  <action>
**Step 1: Create `apps/blog/src/lib/reading-time.ts`**:

```typescript
export function calcReadingTime(text: string): string {
  const words = text.trim().split(/\s+/).filter(Boolean).length
  const minutes = Math.max(1, Math.round(words / 200))
  return `${minutes} min read`
}
```

This implements the CONTEXT.md formula: ~200 words/minute, round to nearest minute, minimum "1 min read".

**Step 2: Create `apps/blog/src/test/reading-time.test.ts`** (RED then GREEN):

Write tests first, run to confirm failure, then verify the implementation passes:
```typescript
import { describe, it, expect } from 'vitest'
import { calcReadingTime } from '../lib/reading-time'

describe('calcReadingTime', () => {
  it('returns "1 min read" for 200 words', () => {
    const text = Array(200).fill('word').join(' ')
    expect(calcReadingTime(text)).toBe('1 min read')
  })

  it('returns "2 min read" for 400 words', () => {
    const text = Array(400).fill('word').join(' ')
    expect(calcReadingTime(text)).toBe('2 min read')
  })

  it('returns "3 min read" for 600 words', () => {
    const text = Array(600).fill('word').join(' ')
    expect(calcReadingTime(text)).toBe('3 min read')
  })

  it('returns minimum "1 min read" for short text', () => {
    expect(calcReadingTime('hello world')).toBe('1 min read')
  })

  it('returns "1 min read" for empty string', () => {
    expect(calcReadingTime('')).toBe('1 min read')
  })

  it('rounds to nearest minute (350 words -> 2 min)', () => {
    const text = Array(350).fill('word').join(' ')
    expect(calcReadingTime(text)).toBe('2 min read')
  })
})
```

**Step 3: Create `apps/blog/src/test/sort.test.ts`**:

Test the post sorting logic that will be used in index.astro:
```typescript
import { describe, it, expect } from 'vitest'

interface PostStub {
  frontmatter: { date: string; title: string }
}

function sortPostsByDate(posts: PostStub[]): PostStub[] {
  return [...posts].sort((a, b) =>
    new Date(b.frontmatter.date).getTime() - new Date(a.frontmatter.date).getTime()
  )
}

describe('sortPostsByDate', () => {
  it('sorts posts newest first', () => {
    const posts: PostStub[] = [
      { frontmatter: { date: '2024-01-15', title: 'B' } },
      { frontmatter: { date: '2023-11-03', title: 'C' } },
      { frontmatter: { date: '2024-04-10', title: 'A' } },
    ]
    const sorted = sortPostsByDate(posts)
    expect(sorted.map(p => p.frontmatter.title)).toEqual(['A', 'B', 'C'])
  })

  it('returns single post unchanged', () => {
    const posts: PostStub[] = [{ frontmatter: { date: '2024-01-01', title: 'Solo' } }]
    expect(sortPostsByDate(posts)).toHaveLength(1)
  })

  it('returns empty array for empty input', () => {
    expect(sortPostsByDate([])).toEqual([])
  })
})
```

**Step 4: Run tests**:
```bash
pnpm --filter @illu/blog test
```
All 9 tests must pass.
  </action>
  <verify>
    <automated>cd /Users/thuyhoang/Conductor/workspaces/illulachy.me/nairobi && pnpm --filter @illu/blog test</automated>
  </verify>
  <acceptance_criteria>
    - apps/blog/src/lib/reading-time.ts contains `export function calcReadingTime`
    - apps/blog/src/lib/reading-time.ts contains `Math.max(1` and `/ 200`
    - apps/blog/src/lib/reading-time.ts contains `min read`
    - apps/blog/src/test/reading-time.test.ts contains `describe('calcReadingTime'` and at least 5 `it(` blocks
    - apps/blog/src/test/sort.test.ts contains `describe('sortPostsByDate'` and `newest first`
    - `pnpm --filter @illu/blog test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Reading time utility exported from lib/reading-time.ts. 6 reading time tests and 3 sort tests pass. BLOG-04 formula validated: 200 wpm, rounded, minimum 1.</done>
</task>

<task type="auto">
  <name>Task 2: Post list page (index.astro) and PostCard component</name>
  <files>
    apps/blog/src/pages/index.astro
    apps/blog/src/components/PostCard.astro
  </files>
  <read_first>
    apps/blog/src/pages/index.astro
    apps/blog/src/layouts/BaseLayout.astro
    apps/blog/src/lib/reading-time.ts
    apps/blog/src/styles/global.css
    packages/content/content/posts/deep-dive-typescript.md
    .stich/layout.jpeg
    .planning/phases/08-blog-foundation/08-UI-SPEC.md
  </read_first>
  <action>
**Step 1: Create `apps/blog/src/components/PostCard.astro`** (per D-12, D-13, UI-SPEC Post List Layout):

```astro
---
interface Props {
  post: {
    slug: string
    frontmatter: {
      title: string
      date: string
      excerpt: string
      category: string
    }
    readingTime: string
  }
}

const { post } = Astro.props
const formattedDate = new Date(post.frontmatter.date).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
})
---

<article class="group py-6 -mx-4 px-4 rounded-lg transition-colors duration-150 ease-out hover:bg-surface-container-low">
  <a href={`/${post.slug}`} class="block no-underline">
    <div class="flex justify-between gap-6 items-start">
      <div class="flex-1 min-w-0">
        <h2 class="font-heading text-4xl font-semibold text-text-primary leading-tight tracking-tight mb-2">
          {post.frontmatter.title}
        </h2>
        <p class="text-text-secondary text-base leading-normal mb-3 line-clamp-2">
          {post.frontmatter.excerpt}
        </p>
        <div class="flex items-center gap-2 text-sm text-text-tertiary tracking-wide">
          <time datetime={post.frontmatter.date}>{formattedDate}</time>
          <span aria-hidden="true">&middot;</span>
          <span>{post.readingTime}</span>
          <span aria-hidden="true">&middot;</span>
          <span class="inline-block bg-interactive-bg-subtle text-interactive-default rounded-full px-2 py-0.5 text-sm">
            {post.frontmatter.category}
          </span>
        </div>
      </div>
    </div>
  </a>
</article>
```

Key design decisions implemented:
- `h2` uses `text-4xl` (36px) Noto Serif semibold per UI-SPEC Heading role
- Excerpt uses `text-base` (16px) Space Grotesk per UI-SPEC Body role
- Metadata uses `text-sm` (14px) with `tracking-wide` per UI-SPEC Label role
- Hover background shifts from `surface-default` to `surface-container-low` (#131313 -> #1C1C1C) per UI-SPEC -- 150ms ease-out
- No borders per D-09 "No-Line Rule"
- Category tag pill uses `bg-interactive-bg-subtle` (#E0AFFF at 10% opacity) + `text-interactive-default` (#E0AFFF) + `rounded-full` per UI-SPEC
- Metadata separators use middle dot (U+00B7) per Copywriting Contract
- `<article>` and `<time datetime>` for semantic HTML per Accessibility Contract
- Thumbnail column omitted when no thumbnail data exists (per UI-SPEC: "When no thumbnail image exists, column collapses")
- Touch target: entire row is clickable via wrapping `<a>` block

**Step 2: Replace `apps/blog/src/pages/index.astro`** (replaces Phase 7 placeholder):

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro'
import PostCard from '../components/PostCard.astro'
import { calcReadingTime } from '../lib/reading-time'

const postModules = import.meta.glob(
  '../../../../packages/content/content/posts/**/*.md',
  { eager: true }
)

interface PostFrontmatter {
  title: string
  date: string
  excerpt: string
  tags: string[]
  category: string
}

const posts = Object.entries(postModules)
  .map(([path, mod]: [string, any]) => ({
    slug: path.split('/').pop()!.replace('.md', ''),
    frontmatter: mod.frontmatter as PostFrontmatter,
    readingTime: calcReadingTime(mod.rawContent?.() ?? ''),
  }))
  .filter(post => post.frontmatter?.title)
  .sort((a, b) =>
    new Date(b.frontmatter.date).getTime() - new Date(a.frontmatter.date).getTime()
  )
---

<BaseLayout>
  <main class="max-w-[680px] mx-auto px-8 max-sm:px-4 py-16">
    <h1 class="font-display text-5xl font-semibold text-text-primary leading-tight tracking-tighter mb-12">
      Writing
    </h1>

    {posts.length === 0 ? (
      <div class="text-center py-16">
        <h2 class="font-heading text-2xl font-semibold text-text-primary mb-4">Nothing here yet</h2>
        <p class="text-text-secondary">Posts are on their way. Check back soon.</p>
      </div>
    ) : (
      <div class="flex flex-col gap-8">
        {posts.map(post => <PostCard post={post} />)}
      </div>
    )}
  </main>
</BaseLayout>
```

Key implementation details:
- Glob path: `../../../../packages/content/content/posts/**/*.md` -- this is 4 levels up from `src/pages/` to reach repo root, per RESEARCH.md Pitfall 2 corrected path calculation: `src/pages/` -> `src/` -> `apps/blog/` -> `apps/` -> repo root -> `packages/content/content/posts/`
- CRITICAL: The glob pattern MUST be a string literal (per Pitfall 1). No variables, no template strings.
- `mod.frontmatter` comes from Astro's default markdown module handling via `import.meta.glob({ eager: true })`
- `mod.rawContent?.()` provides the raw markdown body for reading time calculation
- Posts filtered by `frontmatter?.title` to skip any malformed files
- Sort: `new Date(b.date).getTime() - new Date(a.date).getTime()` for reverse-chronological (per D-15)
- Container: `max-w-[680px]` per UI-SPEC (680px max-width), `px-8` desktop / `max-sm:px-4` mobile padding
- `<h1>` text is "Writing" per Copywriting Contract
- Empty state uses exact copy from Copywriting Contract: "Nothing here yet" / "Posts are on their way. Check back soon."
- Gap between post rows: `gap-8` (32px / `--spacing-8`) per UI-SPEC row separation spec
- Page `<title>` defaults to `Writing -- illulachy.me` from BaseLayout (use em-dash character)

**Step 3: Verify glob path works** -- after creating both files, run `pnpm --filter @illu/blog build`. If build succeeds but post list is empty, the glob path depth is wrong. Try adding or removing one `../` level. The RESEARCH.md calculated 4 levels (corrected from initial 3) but this needs live validation.

If the glob returns empty, debug by temporarily adding `{JSON.stringify(Object.keys(postModules))}` to the template to see what paths Vite resolved.

**Step 4: Verify build** -- `pnpm --filter @illu/blog build` must succeed and the built HTML at `dist/index.html` must contain post titles from the 4 markdown files.
  </action>
  <verify>
    <automated>cd /Users/thuyhoang/Conductor/workspaces/illulachy.me/nairobi && pnpm --filter @illu/blog build && grep -l "Deep Dive" apps/blog/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - apps/blog/src/components/PostCard.astro contains `<article` and `<time datetime=`
    - apps/blog/src/components/PostCard.astro contains `hover:bg-surface-container-low` (hover state per UI-SPEC)
    - apps/blog/src/components/PostCard.astro contains `middot` or `\u00B7` (middle dot separator per Copywriting Contract)
    - apps/blog/src/components/PostCard.astro contains `bg-interactive-bg-subtle` (category tag pill)
    - apps/blog/src/components/PostCard.astro contains `text-4xl` (36px heading per UI-SPEC)
    - apps/blog/src/pages/index.astro contains `import.meta.glob` with a literal string path
    - apps/blog/src/pages/index.astro contains `import BaseLayout` and `import PostCard` and `import { calcReadingTime }`
    - apps/blog/src/pages/index.astro contains `Nothing here yet` (empty state per Copywriting Contract)
    - apps/blog/src/pages/index.astro contains `.sort(` with date comparison logic
    - apps/blog/src/pages/index.astro contains `max-w-[680px]` (per UI-SPEC page container)
    - apps/blog/src/pages/index.astro contains `Writing` as the h1 text
    - `pnpm --filter @illu/blog build` exits 0
    - apps/blog/dist/index.html contains at least one post title (e.g. "Deep Dive" or "TypeScript")
  </acceptance_criteria>
  <done>Post list page renders all 4 blog posts sorted newest-first (April 2024 -> Jan 2024 -> Jan 2024 -> Nov 2023). Each post shows title (Noto Serif 36px), excerpt, metadata row (date, reading time, category tag). Medium-style editorial layout at 680px max-width. Empty state handled. BLOG-01 and BLOG-04 requirements met. Build produces static HTML with post content.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @illu/blog test` -- all unit tests pass (reading time + sort)
- `pnpm --filter @illu/blog build` -- build succeeds
- `grep "Deep Dive" apps/blog/dist/index.html` -- post titles present in built output
- `grep "min read" apps/blog/dist/index.html` -- reading times rendered
</verification>

<success_criteria>
- Post list page loads 4 posts from packages/content/content/posts/ via import.meta.glob()
- Posts displayed in reverse-chronological order (newest first)
- Each post shows title, excerpt, date, reading time, and category tag
- Reading time calculated at 200 wpm with 1 min minimum
- Layout is 680px max-width, centered, with responsive padding
- All unit tests pass (reading time + sort)
- Build produces correct static HTML
</success_criteria>

<output>
After completion, create `.planning/phases/08-blog-foundation/08-02-SUMMARY.md`
</output>
